# java-app-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: java-app-service
  namespace: java-webapp
spec:
  type: ClusterIP
  selector:
    app: java-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
